services:
  usaxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monosites-usaxy
    ports:
      - "8600:80"
    volumes:
      - ./html:/var/www/html
    working_dir: /var/www/html
